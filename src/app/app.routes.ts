import { Routes } from '@angular/router';
import { ThemeViewerComponent } from './theme-viewer/theme-viewer.component';

export const routes: Routes = [
  { path: 'theme-viewer', component: ThemeViewerComponent },
];
